{
  "name": "messaging-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx prisma generate --schema=apps/api/prisma/schema.prisma && turbo build",
    "start:prod": "node apps/api/dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NEXT_TELEMETRY_DISABLED=1 turbo dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "turbo": "^2.8.21"
  },
  "packageManager": "npm@10.5.2"
}
